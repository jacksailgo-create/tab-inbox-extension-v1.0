{
  "name": "tab-inbox-extension",
  "version": "0.9.0",
  "private": true,
  "description": "A local-first Chrome extension dashboard for closing, saving, archiving, and grouping messy browser tabs.",
  "license": "MIT",
  "author": "Henryhan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jacksailgo-create/tab-inbox-extension-v1.0.git"
  },
  "bugs": {
    "url": "https://github.com/jacksailgo-create/tab-inbox-extension-v1.0/issues"
  },
  "homepage": "https://github.com/jacksailgo-create/tab-inbox-extension-v1.0#readme",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json && esbuild src/background/serviceWorker.ts --bundle --format=esm --platform=browser --target=chrome120 --outfile=dist/background/serviceWorker.js",
    "package:extension": "npm run build && node scripts/package-extension.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "esbuild": "^0.28.0",
    "typescript": "^5.8.3"
  }
}
